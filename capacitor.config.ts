import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.jcctech.dailiesIrl',
  appName: 'Dailies IRL',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
